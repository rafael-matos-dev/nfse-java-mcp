# Build
target/
*.class
dependency-reduced-pom.xml

# Segredos e certificados — NUNCA commitar
.env
*.pfx
*.p12
certificados/
Certificados/

# XMLs de notas emitidas (contêm dados reais de contribuinte)
nfse-success-xml/
*-success-xml/

# DANFSe e notas geradas/baixadas (podem conter dados reais).
# PDFs nunca sao commitados. XMLs reais ficam fora do repo ou nos diretorios acima;
# fixtures de teste sao FICTICIOS e versionados em src/test/resources.
*.pdf

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
*.log
